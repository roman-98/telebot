# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: romanstripa
  tag: "fa24d6b-fa24d6b"
  arch: amd64
secret:
  name: "telebot"
  tokenName: "TELE_TOKEN"
  tokenKey: "token"

