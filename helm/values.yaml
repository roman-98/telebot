# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  registry: "ghcr.io"
  repository: "roman-98/telebot"
  os: linux
  arch: amd64
secret:
  name: "telebot"
  tokenName: "TELE_TOKEN"
  tokenKey: "token"

